node_modules/
.next/
out/
dist/
coverage/
test-results/
playwright-report/
.DS_Store
*.log
.env*.local
next-env.d.ts
tsconfig.tsbuildinfo
shots/

# Dist dir for the e2e server, so it can run beside a normal `next dev`.
.next-e2e/

# The tracks live in R2 and are archived outside the repo; keep a local copy
# here for `next dev` and the e2e suite, but never commit it again.
/public/audio/

# Playfair, fetched once so `npm run assets:og` can draw the link preview.
scripts/.cache/
